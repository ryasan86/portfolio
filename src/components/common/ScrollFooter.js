import styled from 'styled-components';

const ScrollFooter = styled.div`
  width: 100%;
  height: 50px;
  background: rgba(0, 0, 0, 0.3);
  position: absolute;
  bottom: 0;
`;

export { ScrollFooter };
